doctype html
html(lang="en")
  head
    link(rel="stylesheet", href="/css/login.css")
  body
    .container
      .login-form
        block content
          h1 Registration
          form(action="/register", method="post", onsubmit="return validatePassword();")
            .form-group
              label(for="username") New Username
              input(type="text", id="username", name="username", placeholder="Enter a new username")
            .form-group
              label(for="password") Password
              input(type="password", id="password", name="password", placeholder="Enter your password")
            .form-group
              label(for="passwordConfirm") Confirm Password
              input(type="password", id="passwordConfirm", name="passwordConfirm", placeholder="Confirm your password")

            // Spaced "Register new User" button
            .register-button-spacing
              button.login-button(type="submit") Register new User

        script.
          function validatePassword() {
            var password = document.getElementById("password").value;
            var confirmPassword = document.getElementById("passwordConfirm").value;

            if (password !== confirmPassword) {
              alert("Passwords do not match. Please try again.");
              return false; // Prevent form submission
            }
            return true; // Allow form submission
          }
        // Existing spacing for "Previous Page" button
        .button-spacing
          button.back-button(type="button", onclick="window.history.back();") Previous Page 
